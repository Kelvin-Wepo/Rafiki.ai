# Backend Dependencies for Rafiki.ai government Voice Assistant
# Python 3.9+ required

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Pydantic for data validation
# v2 is required by google-genai (>=2.0); prebuilt wheels exist for
# Python 3.9-3.12 on manylinux, so no Rust compilation is needed.
pydantic==2.10.3
pydantic-settings==2.1.0

# Security
bcrypt==4.1.2
python-jose[cryptography]==3.3.0

# Google AI Services
google-genai>=0.1.0
google-cloud-dialogflow==2.27.0
google-cloud-texttospeech==2.33.0
google-auth==2.26.2

# Voice Processing
SpeechRecognition==3.10.1
pyttsx3==2.90
pyaudio==0.2.14
pydub==0.25.1  # Audio format conversion (WebM to WAV, etc.)

# Africa's Talking SMS Integration
africastalking==1.2.5

# HTTP Client
httpx==0.26.0
aiohttp==3.9.1
aiofiles==23.2.1  # Async file operations for Wav2Lip

# Database (PostgreSQL with async support)
sqlalchemy[asyncio]==2.0.25
asyncpg==0.29.0
greenlet==3.0.3
psycopg2-binary==2.9.9  # For migrations/sync operations

# Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
cryptography>=41.0.0  # For AES-256-GCM encryption

# Environment Variables
python-dotenv==1.0.0

# Logging and Monitoring
structlog==24.1.0

# Rate Limiting
slowapi==0.1.9

# CORS
starlette==0.35.1

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0
# For Redis-backed fraud service tests (optional in CI)
fakeredis==2.22.0

# Development
black==24.1.0
isort==5.13.2
flake8==7.0.0

# Audio Processing (optional, for advanced voice features)
numpy==1.26.3
# scipy==1.12.0  # requires Fortran compiler; only needed for local dev
# librosa==0.10.1  # depends on scipy

# Image Processing for Avatar Animation
# pillow==10.1.0  # requires build tools; only needed for avatar generation
# opencv-python==4.8.1.78  # depends on pillow

# Face Detection and Alignment for SadTalker
# opencv-contrib-python==4.8.1.78  # depends on opencv-python
# dlib==19.24.4  # requires build tools
# face-alignment==1.3.5  # depends on torch
# mediapipe>=0.10.13  # requires build tools

# SadTalker and Dependencies (when available)
# sadtalker==0.0.1
torch>=2.2.0
torchvision>=0.17.0
torchaudio>=2.2.0

# Video Processing
ffmpeg-python==0.2.0
imageio==2.33.1
imageio-ffmpeg==0.4.9

# Wav2Lip - Lip-sync video generation (alternative to SadTalker)
# Install manually: pip install git+https://github.com/Rudrabha/Wav2Lip.git
# Or use the lighter wav2lip-native package
# wav2lip-native>=0.0.1

# Tensor Operations
tensorboard==2.15.1

# Production Server
gunicorn==21.2.0

# RAG System Dependencies
# rag_service.py uses ChromaDB directly (with its built-in embedding
# functions); langchain / sentence-transformers / tiktoken are not
# imported anywhere in the codebase and were removed because the pinned
# langchain 0.1.0 stack has unresolvable langsmith dependency conflicts.
chromadb==0.4.22
pypdf==3.17.4
beautifulsoup4==4.12.3
